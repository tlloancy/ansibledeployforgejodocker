<VirtualHost *:443>
    ServerName {{ exact_domain_name_can_be_a_sub_domain }}

    ProxyPreserveHost On
    ProxyPass / http://localhost:{{ http_port }}/
    ProxyPassReverse / http://localhost:{{ http_port }}/

    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/{{ exact_domain_name_can_be_a_sub_domain }}/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/{{ exact_domain_name_can_be_a_sub_domain }}/privkey.pem

    ErrorLog ${APACHE_LOG_DIR}/fj-error-ssl.log
    CustomLog ${APACHE_LOG_DIR}/fj-access-ssl.log combined
</VirtualHost>

<VirtualHost *:8083>
    ServerName {{ domain_name }}

    ProxyPreserveHost On
    ProxyPass / http://localhost:{{ http_port }}/
    ProxyPassReverse / http://localhost:{{ http_port }}/

    SSLEngine on
    SSLCertificateFile /etc/letsencrypt/live/{{ exact_domain_name_can_be_a_sub_domain }}/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/{{ exact_domain_name_can_be_a_sub_domain }}/privkey.pem

    ErrorLog ${APACHE_LOG_DIR}/fj-error-8083.log
    CustomLog ${APACHE_LOG_DIR}/fj-access-8083.log combined
</VirtualHost>
